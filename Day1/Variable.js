var a="hello";
var a="raj";  // re declaration is possible with var  and value of a has been updated without causing any error
a="mohan"  // re initailization is possible with var
let b=5;  
//let b=8;  // re declaration is not possible in let but we can do with var 
b=9; // re initailization is possible with let
const c=6;  
//const c=8;   // re declaration is not possible with const ( constant)
//c=8;  // re initailization is not possible with const
console.log(a);
console.log(b);
console.log(c);

