// min and max
console.log(Math.min(4,3,2,1))  // 1
console.log("value of PI", Math.PI)
console.log(Math.round(5.6))  // 6 normal rounding same as math 
console.log(Math.floor(5.6)) // 5 ( lowest nearest integer)
console.log(Math.ceil(5.3)) // 6 (largest nearest integer)
console.log("removes the decimal",Math.trunc(5.8))  // 5 it will remore decimal 
// Math.random to generate random numbers for a range

console.log("value from 0 to <1",Math.random())  // 0 to <1
console.log(Math.floor(Math.random()*(10-1)+1))// Math.random()*(max-min)+min


// guess the number the number game 